- zero_copy:
    no Host_RHEL.m5, Host_RHEL.m6
    only virtio_net
    type = zero_copy
    start_vm = no
    kill_vm = yes
    vhost_net_type = bridge
    variants:
        - enable:
            enable_zerocp = yes
        - disable:
            enable_zerocp = no
